<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bitaim.carromaim">

    <!-- Drawing over other apps (the aim overlay) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground services: overlay + screen capture -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Wake lock keeps overlay alive while playing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Notifications (required on Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme">

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Transparent helper activity for MediaProjection consent -->
        <activity
            android:name=".capture.MediaProjectionRequestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance" />

        <!--
            FloatingOverlayService: no foregroundServiceType needed.
            foregroundServiceType="specialUse" is Android 14 only and crashes
            on Android 5-13. The plain overlay type requires no special subtype.
        -->
        <service
            android:name=".overlay.FloatingOverlayService"
            android:exported="false" />

        <!--
            ScreenCaptureService: foregroundServiceType="mediaProjection" is
            API 29+ only; omitted here for minSdk 21 compatibility.
            The MediaProjection API itself works fine without the manifest attribute
            on Android 5-8.
        -->
        <service
            android:name=".capture.ScreenCaptureService"
            android:exported="false" />

    </application>
</manifest>
